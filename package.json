{
  "name": "default-template",
  "version": "2.0.4",
  "description": "一个用于定义默认值和转换对象结构的实用工具",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "prepublish": "npm run build",
    "test": "npx vitest",
    "test:watch": "npx vitest --watch",
    "test:ui": "npx vitest --ui",
    "test:cov": "npx vitest run --coverage"
  },
  "keywords": [
    "template",
    "default",
    "object",
    "transform",
    "utility",
    "默认值",
    "模板"
  ],
  "author": "yuantao_npm <69455804@qq.com>",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^24.0.7",
    "@vitest/coverage-v8": "^3.2.4",
    "@vitest/ui": "^3.2.4",
    "typescript": "^4.9.5",
    "vitest": "^3.2.4"
  }
}
